# Roadmap & Known Issues

## Known Issues

### Critical — Firmware

*All resolved — see Resolved section below.*

### Critical — Python

- ~~**PY-CRIT-1**: `sys.exit()` in library code~~ (fixed: replaced with `ConfigError` exception; `main.py` catches at entry point; 2026-02-28)
- ~~**PY-CRIT-2**: Path traversal vulnerability~~ (fixed: `resolve().is_relative_to()` containment in both headless and Gradio paths + `_write_profile` defense-in-depth; 33 security tests; 2026-02-28)
- ~~**PY-CRIT-3**: Falsy-zero threshold bug~~ (fixed: removed redundant `or DEFAULT` fallbacks; thresholds already resolved via `coerce_float()`; 4 regression tests; 2026-02-28)

### High Priority

- ~~**PY-HIGH-1**: Unhandled `base64.b64decode()` in `audio_router.py:41` — malformed delta crashes audio loop~~ (fixed: try/except + odd-byte guard; 5 tests; 2026-02-28)
- **PY-HIGH-2**: Tool registry globals (`ALL_TOOLS`, `ALL_TOOL_SPECS`, `_TOOLS_INITIALIZED`) have no locking (`core_tools.py:38-40`)
- ~~**PY-HIGH-3**: Auto light_context dispatch has no independent timeout~~ (fixed: `asyncio.wait_for()` with configurable timeout; 1 test; 2026-02-28)
- **PY-HIGH-4**: HeadWobbler TOCTOU race — generation check outside lock (`head_wobbler.py:78-81`); queue drain in `reset()` can violate `Queue` contract
- ~~**PY-HIGH-5**: Thread leak in `MovementManager.start()`~~ (fixed: `_lifecycle_lock` serialises `start()`/`stop()`; 7 tests; 2026-03-01)
- ~~**PY-HIGH-6**: Type annotation error — `robot: ReachyMini = None` should be `ReachyMini | None = None`~~ (fixed; 2026-02-28)
- ~~**FW-HIGH-1**: COBS encode overflow silently drops packets~~ (fixed: `txDropCount` incremented on drop, surfaced via EVT_DIAG; 2026-02-28)
- ~~**FW-HIGH-2**: No sensor error telemetry~~ (fixed: `mmwaveFailCount`/`mmwaveConsecutiveFails` counters + periodic EVT_DIAG emission; 2026-02-28)
- ~~**FW-HIGH-3**: No rate-limiting on EVT_STATE emission~~ (fixed: 200 ms minimum interval between change-triggered emissions; 2026-02-28)

### Medium Priority

- **PY-MED-1**: `GET /personalities/save_raw` has side effects (writes files) — should be POST (`headless_personality_ui.py:191-209`)
- **PY-MED-2**: Duplicate sample rate assignments in `openai_realtime.py:73-80`
- **PY-MED-3**: `last_activity_time` initialized before event loop in `openai_realtime.py:85`; no lock for concurrent access
- **PY-MED-4**: Float equality comparison for timestamps in `camera_worker.py:175`
- **PY-MED-5**: Public `move_queue` bypasses thread-safe command queue (`moves.py:307`)
- **PY-MED-6**: API key polling loop (0.2 s `time.sleep`) in `console.py:376-385`
- **PY-MED-7**: `launch()` overloaded (~63 lines) in `console.py:322-385`
- **PY-MED-8**: Unbounded analytics JSONL growth in `light_orchestrator.py`
- **PY-MED-9**: mmWave serial glob patterns ordered for macOS, not current platform
- **PY-MED-10**: Inconsistent error handling (error dicts vs exceptions vs `sys.exit()`)
- ~~**FW-MED-1**: BH1750 light sensor init is one-shot~~ (fixed: periodic re-init every 10s on failure; 2026-03-01)
- ~~**FW-MED-2**: 19 unstructured global variables~~ (fixed: 6 flat structs — VitalsCache, PresenceState, EmitTimers, DiagCounters, HostSettings, PrevState; 2026-03-01)
- ~~**FW-MED-3**: Buffer sizes undocumented~~ (fixed: sizing rationale + `static_assert` for MAX_TARGETS_WIRE; 2026-03-01)
- **FW-MED-4**: Guard rails (BR/HR/distance) are compile-time only; no runtime configurability
- ~~**FW-MED-5**: `guessPose()` has no hysteresis~~ (fixed: ±5cm dead zone with previous-pose hold; 2026-03-01)

### Low Priority

- **PY-LOW-1**: IdlePolicy constructor doesn't validate parameters (negative timings silently break)
- **PY-LOW-2**: `_safe_load_obj()` returns `{}` on parse errors without logging
- **PY-LOW-3**: `_load_module_from_file()` pollutes `sys.modules` before `exec_module()` succeeds
- **PY-LOW-4**: `sweep_look.py` has no error handling; magic `0.9 * np.pi` differs from mmWave's `0.8`
- **PY-LOW-5**: Three personality save endpoints (redundant GET variant)
- **FW-LOW-1**: `0x00` frame delimiter not a named constant
- **FW-LOW-2**: No firmware unit tests (state machine, COBS, CRC)

### Resolved

- ~~**FW-CRIT-1**: Vitals hysteresis bypass in `emitBio()`~~ (fixed: `br_emit_ok`/`hr_emit_ok` now require `vitalsStreak >= VITALS_CONFIRM`; 2026-02-28)
- ~~**FW-CRIT-2**: Stale vitals under fallback lock~~ (fixed: 2s expiry on `lastBR`/`lastHR` via `VITALS_CACHE_EXPIRY_MS`; 2026-02-28)
- ~~**FW-CRIT-3**: Race condition in presence detection~~ (fixed: `dist_ok` recomputed after fallback via `isFinitePositive(dist_cm)`; 2026-02-28)
- ~~**FW-HIGH-1**: COBS encode overflow silently drops packets~~ (fixed: `txDropCount` incremented, surfaced via EVT_DIAG; 2026-02-28)
- ~~**FW-HIGH-2**: No sensor error telemetry~~ (fixed: cumulative counters + periodic EVT_DIAG every 10s; 2026-02-28)
- ~~**FW-HIGH-3**: No rate-limiting on EVT_STATE~~ (fixed: 200ms minimum interval between change-triggered emissions; 2026-02-28)
- ~~Handler logic covered via IdlePolicy/LightOrchestrator/TranscriptHandler/ToolDispatcher/AudioRouter tests, but the streaming loop in `openai_realtime.py` still has low direct coverage~~ (fixed: 71 tests; 2026-02-24)
- ~~Race condition in antenna blending: false positive — fixed `get_status()` to use `_shared_is_listening`~~ (2026-02-24)
- ~~Session setup exception returns early but connection object may already be set~~ (fixed: try/finally; 2026-02-24)
- ~~Idle tool call flag not reset on exception path~~ (fixed; 2026-02-24)
- ~~Light baseline save throttled with dirty flag + 60s interval~~ (2026-02-24)
- ~~Lux extraction paths verified correct~~ (2026-02-24)
- ~~Tool registry runs disk I/O and can `sys.exit(1)` at import time~~ (fixed: lazy init + `ToolRegistryError`; 2026-02-24)
- ~~`tools.txt` entries silently skipped~~ (fixed: post-registration validation; 2026-02-24)
- ~~Gradio pinned to dev version~~ (fixed: `>=5.50.0` stable floor; 2026-02-24)
- ~~mypy only covers `src/`~~ (fixed: added `tests/`; 2026-02-24)
- ~~`requires-python >=3.10` vs mypy 3.12 mismatch~~ (fixed: aligned to `>=3.12`; 2026-02-24)
- ~~Unbounded per-hour baseline growth~~ (fixed: stale user entries pruned; 2026-02-24)
- ~~Float precision loss in cost accumulation~~ (fixed: `decimal.Decimal`; 2026-02-24)
- ~~Magic numbers in moves.py~~ (fixed: extracted as named constants; 2026-02-24)
- ~~Dead commented-out code in HeadWobbler~~ (removed; 2026-02-24)
- ~~IdlePolicy state machine transitions undocumented~~ (documented; 2026-02-24)
- ~~BIO state field captured but never used~~ (fixed: `device_state` surfaced; 2026-02-24)
- ~~Targets truncation flag extracted but never surfaced~~ (fixed: surfaced in dashboard; 2026-02-24)

---

## Roadmap

### Phase 1: Critical Fixes (v0.2.3)

**Goal**: Fix all critical and security issues. Zero user-facing behavior changes.

#### Firmware (3 items) — DONE (2026-02-28)
1. ~~Fix `emitBio()` to require `vitalsStreak >= VITALS_CONFIRM` for `br_emit_ok`/`hr_emit_ok`~~
2. ~~Add 2-second expiry to cached `lastBR`/`lastHR` — clear on timeout~~
3. ~~Restructure presence detection to check `isFinitePositive(dist_cm)` independent of stale `dist_ok` flag~~

#### Python (3 items)
4. ~~Replace `sys.exit()` in `config.py` and `prompts.py` with `ConfigError`/`RuntimeError` exceptions; handle in `main.py`~~
5. ~~Add path traversal validation in `headless_personality.py` and `gradio_personality.py` using `resolve().is_relative_to()`~~
6. ~~Replace all `value or DEFAULT` patterns with `value if value is not None else DEFAULT` in `light_context.py` and audit for same pattern elsewhere~~

#### Tests (3 items)
7. ~~Add falsy-zero regression test for `light_context.py` thresholds~~
8. ~~Add path traversal security test for personality management~~
9. ~~Add AudioRouter test for malformed base64 input~~

### Phase 2: Stability & Thread Safety (v0.3.0)

**Goal**: Harden concurrency, close high-priority gaps, improve error resilience.

#### Audio & Movement (4 items)
1. Wrap `base64.b64decode()` in try/except in `audio_router.py`
2. Fix HeadWobbler TOCTOU: acquire lock before generation check; bound queue; fix `reset()` drain
3. Guard `MovementManager.start()` against thread leak; make `move_queue` private
4. Document threading contracts in `head_wobbler.py` and `camera_worker.py` module docstrings

#### Tool System (3 items)
5. Add `threading.Lock` to tool registry initialization in `core_tools.py`
6. ~~Add independent `asyncio.wait_for()` timeout to auto light_context dispatch~~ (2026-02-28)
7. Standardize tool error handling: error dicts for tools, exceptions for libraries

#### Firmware (3 items) — DONE (2026-02-28)
8. ~~Send `EVT_ERR` on COBS encode overflow instead of silent drop~~ (used diagnostic counter + EVT_DIAG instead)
9. ~~Add `mmWave.update()` failure counter; emit periodic health telemetry~~
10. ~~Rate-limit `EVT_STATE` emission to max once per 200 ms on state change~~

#### REST API (2 items)
11. Change `GET /personalities/save_raw` to POST
12. ~~Fix type annotation `robot: ReachyMini | None = None` in `main.py`~~ (2026-02-28)

#### Tests (4 items)
13. Add multi-threaded tests for `moves.py` (command queue, start/stop lifecycle)
14. Add multi-threaded tests for `head_wobbler.py` (reset race, generation safety)
15. ~~Add ToolDispatcher tests for light_context timeout~~ (2026-02-28) and malformed sensor state
16. Replace `asyncio.sleep()` timing in tests with deterministic mechanisms

### Phase 3: Code Quality & Maintainability (v0.3.x)

**Goal**: Clean up structural debt, improve developer experience.

#### Python Refactoring (7 items)
1. Remove duplicate sample rate assignments in `openai_realtime.py`
2. Defer `last_activity_time` init to `start_up()`; add lock for concurrent access
3. Fix float equality comparison in `camera_worker.py`
4. Replace API key polling loop with event-based signaling in `console.py`
5. Decompose `console.py:launch()` into smaller methods
6. Consolidate personality REST save endpoints (remove redundant GET variant)
7. Platform-aware serial port glob ordering in mmWave `_resolve_serial_port()`

#### Firmware Refactoring (5 items)
8. Encapsulate globals into `PresenceState`, `VitalsState`, `TimingState` structs
9. Document buffer sizing rationale; add overflow assertions for `emitTargets()`
10. Add hysteresis to `guessPose()` (dead zone around 55 cm)
11. Name `0x00` frame delimiter as `FRAME_DELIMITER` constant
12. Add BH1750 periodic re-init on failure

#### Observability (2 items)
13. Add analytics JSONL rotation or size cap in `LightOrchestrator`
14. Validate IdlePolicy constructor parameters (reject negative/zero timings)

#### Low-Priority Cleanup (4 items)
15. Log warning in `_safe_load_obj()` on parse errors
16. Defer `sys.modules` insertion in `_load_module_from_file()` until after `exec_module()`
17. Add error handling to `sweep_look.py`; synchronize angle constants with mmWave
18. Convert test factory functions to proper pytest fixtures

### Phase 4: Firmware v2 (v0.4.0)

**Goal**: Runtime configurability, diagnostics, and testability for firmware.

1. Add `CMD_SET_GUARD_RAILS` command — runtime-configurable BR/HR ranges, near-zone distance, movement threshold
2. ~~Add `EVT_DIAG` message type — expose error counters~~ (done: mmWave fail count, consecutive fails, TX drops; SNR/I2C health deferred; 2026-02-28)
3. Implement soft-reset command (`CMD_RESET`) for recovery from sensor/I2C failures without power cycle
4. Add firmware unit test harness — state machine, COBS codec, CRC testable on-host
5. Decouple state machine from sensor polling rate — run at fixed wall-clock frequency

### Future Vision

#### Completed

- Handler decomposition: 5 standalone testable classes extracted from `_run_realtime_session()` — IdlePolicy, LightOrchestrator, TranscriptHandler, ToolDispatcher, AudioRouter (2026-02-23)
- Non-blocking tool dispatch with `asyncio.create_task()` + `Semaphore(1)` + `wait_for()` timeout (2026-02-23)
- Protocol version handshake rejection at session start (2026-02-26)
- Graceful degradation when mmWave sensor disconnects mid-session (2026-02-24)
- Integration tests for async streaming loop with mock WebSocket (2026-02-24)
- Boundary condition tests for bio rate filtering (2026-02-26)
- File I/O error tests for LightOrchestrator persistence (2026-02-26)
- Multi-person tracking: IdlePolicy multi-target awareness, scan-only mode, system prompt hint (2026-02-26)
- Device state context: `build_device_context()` enriches mmWave results with vitals reliability and transitions (2026-02-27)
- Sensor dashboard in headless UI polls `/sensor` for live target count, vitals, lux, and device state (2026-02-24)
- Full-stack code review: 44 issues across firmware and Python, documented with severity and file references (2026-02-27)

#### Aspirational (beyond v0.4)

- **Adaptive guard rails**: Firmware learns personalized vitals baselines over time and adjusts thresholds
- **Multi-person vitals**: When target isolation improves in future sensor firmware, support per-person vitals tracking
- **Over-the-air firmware updates**: Host Python code pushes firmware updates to XIAO over USB
- **Sensor fusion**: Combine mmWave radar with camera vision for more robust person tracking
- **Health trend analysis**: Long-term vitals storage with trend detection and wellness insights
- **Plugin architecture**: Full external tool/profile marketplace with versioning and dependency management
