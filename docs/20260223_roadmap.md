# Roadmap & Known Issues

## Known Issues

### High Priority

- ~~Handler logic covered via IdlePolicy/LightOrchestrator/TranscriptHandler/ToolDispatcher/AudioRouter tests, but the streaming loop in `openai_realtime.py` still has low direct coverage~~ (fixed: 71 tests covering all public methods — receive, emit, send_idle_signal, event loop routing, shutdown, apply_personality, restart_session, helpers, API key persistence; 2026-02-24)
- ~~Race condition in antenna blending: false positive — all writes are on the worker thread via command queue; fixed `get_status()` to use `_shared_is_listening` (2026-02-24)~~
- ~~Session setup exception returns early but connection object may already be set — fixed: try/finally around event loop clears connection + connected_event (2026-02-24)~~

### Medium Priority

- ~~Idle tool call flag (`is_idle_tool_call`) not reset on exception path — fixed: reset in emit() exception handler (2026-02-24)~~
- ~~Light baseline save throttled with dirty flag + 60s interval; flush at shutdown (2026-02-24)~~
- ~~Lux extraction paths verified correct; regression tests added (2026-02-24)~~
- ~~Tool registry runs disk I/O and can `sys.exit(1)` at import time — fixed: lazy init + `ToolRegistryError` replaces `sys.exit(1)` (2026-02-24)~~
- ~~`tools.txt` entries silently skipped — fixed: post-registration validation warns on unregistered tool names (2026-02-24)~~
- ~~Gradio pinned to dev version — fixed: `>=5.50.0` stable floor (2026-02-24)~~
- ~~mypy only covers `src/` — fixed: added `tests/` (2026-02-24)~~
- ~~`requires-python >=3.10` vs mypy 3.12 mismatch — fixed: aligned to `>=3.12` (2026-02-24)~~

### Low Priority

- ~~Unbounded per-hour baseline growth in LightOrchestrator — fixed: stale user entries pruned on load (configurable via `HEALTHY_LIGHT_BASELINE_MAX_AGE_DAYS`, default 90) (2026-02-24)~~
- ~~Float precision loss in cost accumulation (openai_realtime.py:43-55) — fixed: cost constants and accumulator converted to `decimal.Decimal` (2026-02-24)~~
- ~~Magic numbers in moves.py timing constants without documentation (lines 266-282) — fixed: extracted as documented module-level constants (2026-02-24)~~
- ~~Dead commented-out code in HeadWobbler (head_wobbler.py:149-157) — removed (2026-02-24)~~
- ~~IdlePolicy state machine transitions undocumented — documented: full state diagram and transition table in `idle_policy.py` module docstring; summaries in README.md and CLAUDE.md (2026-02-24)~~
- ~~BIO state field captured but never used — fixed: `device_state` surfaced in scan/measure results; sensor dashboard added to headless UI (2026-02-24)~~
- ~~Targets truncation flag extracted but never surfaced to user — fixed: `targets_truncated` and `max_target_count` surfaced in scan results; displayed in sensor dashboard (2026-02-24)~~

### Firmware

(all firmware hardening items completed — see TODO.md)

## Future Vision

### Handler Decomposition (complete)

All major concerns extracted from `_run_realtime_session()` into standalone, testable classes: IdlePolicy, LightOrchestrator, TranscriptHandler, ToolDispatcher, AudioRouter. The event loop is now a ~80-line thin router. See `docs/plans/2026-02-23-decompose-session-design.md`.

### Robustness

- Non-blocking tool dispatch with `asyncio.create_task()` + `Semaphore(1)` + `wait_for()` timeout (complete)
- ~~Protocol version handshake rejection at session start~~ (complete: `_handshake_version()` sends CMD_PING, validates first frame's version byte, fails fast on mismatch/timeout; defense-in-depth drop-and-warn retained in `_poll_events()`; 2026-02-26)
- ~~Graceful degradation when mmWave sensor disconnects mid-session~~ (complete: error tracking in IdlePolicy with backoff suppression, error propagation to sensor_state/dashboard, auto-recovery on reconnect; 2026-02-24)

### Testing

- ~~Integration tests for the async streaming loop with mock WebSocket~~ (complete: `_EventConn` + `_run_session_with_events` harness drives 20+ event loop tests; 2026-02-24)
- ~~Boundary condition tests for bio rate filtering (exact guard-rail values)~~ (complete: 22 tests — decode at exact firmware guard rails BR 4–30 / HR 35–200, null sentinels, wire edge cases, tool acceptance gate; 2026-02-26)
- ~~File I/O error tests for LightOrchestrator persistence (corrupted JSON, permissions)~~ (complete: 21 tests — corrupted/truncated/binary JSON, non-dict repair, permission errors on read/write/flush, analytics write failures, prune edge cases; 2026-02-26)

### Sensor Intelligence

- ~~Multi-person tracking: targets truncation flag and target count now surfaced in scan results and dashboard; next step is behavioral logic for >1 target~~ (complete: IdlePolicy multi-target awareness with configurable interval multiplier, scan-only mode, ToolDispatcher routing, system prompt hint; 2026-02-26)
- ~~BIO state field (`device_state`) now surfaced in scan/measure results and dashboard; next step is richer context integration~~ (complete: `build_device_context()` enriches mmWave results with vitals reliability, state transitions, and contextual notes; system prompt updated with interpretation guidance; 2026-02-27)
- Sensor dashboard in headless UI polls `/sensor` endpoint for live target count, vitals, lux, and device state
